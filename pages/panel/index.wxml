<view class="demo-page">
  <!-- 基础用法 -->
  <view class="demo-block">
    <text class="demo-block__title">基础用法</text>
    <view class="demo-block__content">
      <ec-panel title="{{basicPanel.title}}" desc="{{basicPanel.desc}}">
        <view class="panel-content">
          <text>这里是面板的主体内容区域</text>
          <text>可以放置任意内容</text>
        </view>
      </ec-panel>
    </view>
  </view>

  <!-- 无边框面板 -->
  <view class="demo-block">
    <text class="demo-block__title">无边框面板</text>
    <view class="demo-block__content">
      <ec-panel title="{{noBorderPanel.title}}" desc="{{noBorderPanel.desc}}" border="{{false}}">
        <view class="panel-content">
          <text>这是一个没有边框的面板</text>
          <text>适用于嵌入其他容器的场景</text>
        </view>
      </ec-panel>
    </view>
  </view>

  <!-- 无内边距面板 -->
  <view class="demo-block">
    <text class="demo-block__title">无内边距面板</text>
    <view class="demo-block__content">
      <ec-panel title="{{noPaddingPanel.title}}" desc="{{noPaddingPanel.desc}}" padding="{{false}}">
        <view class="panel-content-no-padding">
          <text>这是一个没有内边距的面板</text>
          <text>内容紧贴边界</text>
        </view>
      </ec-panel>
    </view>
  </view>

  <!-- 带底部面板 -->
  <view class="demo-block">
    <text class="demo-block__title">带底部面板</text>
    <view class="demo-block__content">
      <ec-panel 
        title="{{footerPanel.title}}" 
        desc="{{footerPanel.desc}}" 
        footer="{{footerPanel.footer}}"
      >
        <view class="panel-content">
          <text>带有底部区域的面板</text>
          <text>适用于需要额外说明或操作的场景</text>
        </view>
      </ec-panel>
    </view>
  </view>

  <!-- 自定义样式 -->
  <view class="demo-block">
    <text class="demo-block__title">自定义样式</text>
    <view class="demo-block__content">
      <ec-panel
        title="自定义面板"
        desc="自定义颜色和圆角"
        background="#f9f9f9"
        title-color="#1989fa"
        desc-color="#999"
        radius="16rpx"
      >
        <view class="panel-content">
          <text>通过属性自定义面板样式</text>
          <text>满足不同场景的需求</text>
        </view>
      </ec-panel>
    </view>
  </view>

  <!-- 选项说明 -->
  <view class="demo-block">
    <view class="demo-block__content demo-block__content--desc">
      <text class="demo-desc__title">面板 Panel</text>
      <text class="demo-desc__text">用于组织和展示内容的容器组件，常用于信息分组、表单区域等场景。支持标题、描述、底部区域等配置，可自定义边框、内边距、颜色等样式。</text>
    </view>
  </view>
</view>
