/* ec-row 样式 */
.ec-row {
  position: relative;
  box-sizing: border-box;
}

.ec-row::before,
.ec-row::after {
  display: table;
  content: '';
}

.ec-row::after {
  clear: both;
}

/* ec-col 样式 */
.ec-col {
  position: relative;
  box-sizing: border-box;
  min-height: 1px;
}
